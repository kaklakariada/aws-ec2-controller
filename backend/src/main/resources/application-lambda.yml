micronaut:
    server:
        cors:
            enabled: true
            configurations:
                all-prod:
                    allowedMethods:
                        - GET
                        - POST
                        - PUT
                    allowedHeaders:
                        - Authorization
                    allowCredentials: true
                    maxAge: 1800 # 30 min